import { environment } from './environment';

/**
 * Class to manage environment variables
 */
export class HasEnvironment {
  /**
   * Angular app environment
   */
  public environment = environment;
}
